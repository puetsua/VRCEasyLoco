# Getting Started

Open `Tools -> EasyLoco`, assign a `VRCAvatarDescriptor`, and review the avatar's VRChat animation layer slots.

This initial version is read-only. Future versions should add explicit edit flows for locomotion-related controller changes.

